<%= form_with(model: blog_feature, local: true) do |form| %>
  <% if blog_feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog_feature.errors.count, "error") %> prohibited this blog_feature from being saved:</h2>

      <ul>
      <% blog_feature.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :blog_feature_title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :blog_feature_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
